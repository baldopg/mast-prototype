# Copia este archivo como .env para desarrollo local con `netlify dev`.
# El .env NUNCA se sube a git (ya está en .gitignore).
# En producción, estas variables se configuran en el panel de Netlify:
# Site configuration > Environment variables

GEMINI_API_KEY=tu_clave_de_google_ai_studio

# Opcional. Si el modelo por defecto da error 404, prueba con otro nombre.
# GEMINI_MODEL=gemini-2.5-flash
